#include "server.h"
#include <stdio.h>

void initialize_server(const char *host, int port) {
    // TODO: Inicializar HTTP server (p. ej. con libmicrohttpd o mongoose)
    // Registrar rutas: POST /login, POST /register, GET /matches
    printf("Server initialized at %s:%d (pendiente de implementación)\n", host, port);
}